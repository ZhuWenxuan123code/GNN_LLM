# 连续研究地图

## 当前阶段

阶段 1：建立代表性方法与结构证据的基线地图，识别现有 Graph-LLM 工作在“结构可辨识、置换一致、因果利用、跨拓扑泛化”四个层次上的证据缺口。

## 候选论文 Idea 路由

本文件只维护假设、证据账本和未解决问题。候选论文 Idea 的去重、评分、状态和实验准备统一维护在 [`idea_backlog.md`](idea_backlog.md)，避免把研究假设与实现方案混为一谈。当前优先候选为 I1“置换一致且可定位干预的离散图 Tokenizer”；每日任务应优先强化、反驳或执行已有候选，而不是持续增加重复 Idea。

## 当前工作假设

| ID | 假设 | 状态 | 支持证据 | 反对证据 | 最小验证 |
|---|---|---|---|---|---|
| H1 | 许多 GNN-LLM 方法证明了结构输入或任务增益，但没有证明 LLM 因果性地使用了结构 | 部分支持 | Zhang et al. (2026, arXiv:2605.03514) 用 GTEval 复测 6 个 GTokenLLM，发现原任务准确率、GT 可解码和跨层 attention 与 instruction/content 变化下的可靠结构理解并不一致；PRBCD 下 GraphGPT/TEA-GLM 基本不变，提示文本捷径。Herbst et al. (2026, arXiv:2511.10234) 进一步显示纯 LLM graph reasoner 的固定格式准确率会依赖节点位置、edge locality 与 syntax | GT 在 LLaGA 上保留了可用于 link prediction 的结构信息，且各层确实关注 GT；Lost in Serialization 也显示较大模型在若干变体上更稳，但这些都不构成结构因果使用证据 | 对代表性方法执行 permutation、语义保持的 topology counterfactual、text shuffle 与最终输出干预的统一证据审计 |
| H2 | 节点重编号和序列化顺序可能导致 Graph-LLM 学到顺序捷径而非图不变量 | 部分支持 | Herbst et al. (2026, arXiv:2511.10234) 对同一图采样 10 次 node relabeling，并测试 edge-list/adjacency-list/matrix、edge ordering 与 syntax；G1 在部分任务上对重编号和结构顺序更脆弱，`bipartite_maximum_matching` 暴露了编号连续性捷径；EDT-Former 的 entropy patching 依赖 canonical SMILES 顺序并将序列片段映射回图节点，但未测等价 SMILES 或 node relabeling；Thushalika et al. (2026, arXiv:2606.09484) 进一步发现三种 LLM 在普通 graph-isomorphism 检测上接近满分，却在同拓扑随机重标记上只有 0-39.2%；GraphTokenizer 以 deterministic frequency-guided serialization 作为缓解 ordering ambiguity 的接口设计，但未做专门的 relabeling 实测 | gpt-oss-120B 对多种 syntax 更稳，部分 G1 的 output variability 下降可能来自准确率提升；EDT-Former 的 graph encoder 与 pooling 可能缓解部分编号影响；GraphTokenizer 的重复 label、起点/方向和 lexical tie-break 仍可能留下实现依赖；尚未在 GNN-LLM/GTokenLLM 上做统一等价序列化实验，也未穷尽同构类 | 汇总 permutation test，并设计同构图对照；同时报告 accuracy shift、output span 和最终结构干预方向正确率 |
| H3 | 显式、多尺度、可干预的结构瓶颈可能比单一 projector 更适合验证结构利用 | 待验证 | GraphTokenizer 将 edge-covering reversible serialization 与 BPE 组合成可解码的 discrete structural token，ZINC 词表中 4--6 node token 占 41.5%，并在纯结构 COIL-DEL 上优于 textualized GraphGPT/LLAGA；这支持“显式结构瓶颈值得测试”，但不是 causal-use 证据 | 该接口仍未提供 fixed-text topology intervention、target-level addressability、hidden-state mediation 或 GNN/LLM/readout responsibility split；continuous features 需额外量化/融合，可能破坏无损性 | 在相同 Transformer 和 matched graph pairs 上比较 discrete reversible bottleneck、continuous projector 与 raw serialization，联合测 decode fidelity、permutation、关键/无关干预和 topology-OOD |
| H4 | 只有同时通过 instruction invariance、结构反事实敏感性和 shortcut controls，才能把 graph-token 能力称为因果有效的图结构理解 | 待验证 | GTEval 暴露了 instruction/template 脆弱性；PRBCD 结果显示不同模型对结构扰动的响应分化；作者同时报告 GT 的 text/structure decodability 与 attention，形成多层证据缺口 | 尚无同一模型、同一批样本上的联合评测；GTEval 的 instruction 改写不是 graph counterfactual，PRBCD 也未保持任务语义 | 在同一模型上做 `instruction variants × node permutation × same-text topology counterfactual × text shuffle`，并与 text-only、graph-only、shortcut baseline 比较 |
| H5 | permutation robustness 与 causal topology use 是可分离的能力；前者稳定不代表后者成立 | 部分支持 | Lost in Serialization 将 node labeling、computational structure、syntax 与 spectral generalization 分开，说明等价编码鲁棒性和未见性质泛化并非同一维度；IP-GLLM 也把 confounding/OOD 稳定性与 latent-space intervention 分开处理；Thushalika et al. (2026, arXiv:2606.09484) 在同一纯序列化 LLM 评测中观察到 ordinary isomorphism accuracy 近满分而 permutation-invariance 只有 0-39.2%；GraphTokenizer 又把 determinism/reversibility 作为离散接口属性，但没有将其与最终 causal intervention 联合测量 | 尚无同一 Graph-LLM 上同时满足等价变换稳定、关键结构干预方向正确、无关干预稳定和 topology-OOD 的结果；GraphTokenizer 也没有 graph-level causal intervention 或 LLM/GNN responsibility split | 在同一批图上交叉执行 `10 relabelings × edge-order/syntax variants × same-text topology counterfactual`，并报告 invariant stability、intervention direction accuracy 与 held-out topology-family accuracy |
| H6 | latent-space soft intervention 的 OOD 增益不等于 LLM 对 causal topology 的 graph-level 因果利用 | 待验证 | IP-GLLM 用 causal tokenizer、REC-Context、interventional prompts 和 confounding-controlled OOD/transfer 取得间接证据；但其 (do(\cdot)) 是 embedding-space approximation，而不是同一图的 matched topology intervention | 尚无 graph-level matched counterfactual、GNN-only/LLM-only 和 direction-correctness 联合结果；attention visualization 不能替代因果验证 | 固定节点文本/特征和标签语义，构造 causal-ring/star-confounder matched graph pairs，做关键/无关边干预、node permutation，并与 base Graph-LLM、IP-GLLM、GNN-only、LLM-only 比较 |
| H7 | 架构级 permutation robustness 可以由局部结构 encoder、pooling 和全局 readout 提供，而不代表后续 LLM 形成了因果拓扑表征 | 待验证 | HLM-G 在 10 次 node relabeling 上取得低 accuracy drop，并通过 local structure embedding similarity 和 pooling ablation 提供结构编码证据；但其主实验是专用 hierarchical encoder/classifier，attention/Recall@k 不能证明 causal use | 尚无同一模型上的 fixed-text topology counterfactual、decoder-only 与 encoder/readout-only 分离，也没有证明该鲁棒性迁移到 GNN-LLM/GTokenLLM | 在 HLM-G、GraphToken、IP-GLLM 上交叉执行 node relabeling、edge-order、关键/无关拓扑干预、text shuffle 和 held-out topology family，并比较 encoder-only/readout-only/LLM-only |
| H8 | LLM-generated semantic counterfactual instructions 可以提升 GNN explanation 的可行性，但不蕴含 LLM 对 topology 的因果利用 | 待验证 | LLM-GCE 在 GT-GNN 指导的 graph counterfactual 上改善 feasible validity/proximity；direct GPT-4 counterfactual generation validity 为 0；CA 同时接收 text embedding 与 GT-GNN embedding，dynamic feedback 依赖 GT-GNN probability | 尚无 q_G removal/shuffle、text shuffle、fixed-text topology pair、LLM/GNN responsibility separation 或 held-out topology-family 结果 | 固定节点文本/属性与任务语义，只干预关键/无关 topology，比较 LLM-GCE、q_G-shuffle、text-only、GNN-only、CA/readout-only 与 Graph-LLM 的 direction accuracy、permutation consistency 和 topology OOD |
| H9 | Graph-LLM 的 encoding/order robustness 与 topology intervention sensitivity 是两个近似正交轴；通过一个轴不能推出通过另一个轴 | 待验证 | CausalGraph2LLM 在 7 种 encoding 上观察到最高约 60% 的性能变化，BFS/BFS-R 顺序会改变 source/sink 结果，且 downstream `do(X=x)` 任务同样随 encoding 变化；random-vs-semantic labels 暴露了 pretraining shortcut | 该证据来自纯序列化 LLM causal DAG benchmark，尚无同一 Graph-LLM 上的 node permutation、fixed-text topology pair、关键/无关干预和 component responsibility split 联合结果 | 在固定节点文本/特征和任务语义的 matched pairs 上交叉执行 `10 relabelings × encoding/order variants × key/irrelevant topology intervention × text shuffle`；若无关干预不稳定、关键干预方向不正确或 readout/text-only 可复现全部结果，则否定 H9 |
| H10 | Graph-LLM 的 OOD 能力应沿 semantic、graph-size、generator/transitivity、cross-task 和 real-world 五个轴分解；同 task/同 size 的稳定性不能称为 topology-generalization | 待验证 | NLGift 在 37,000 个问题上将四个 graph task 与五类 pattern transfer 组合；semantic/numeric 迁移相对可行，但 reasoning transfer 无 Strong Recovery，graph size 只有 2/8 设置达到 Strong Recovery，synthetic tuning 对 real-world tasks 几乎无普遍收益；OOD-GraphLLM 又在 scaffold-based 与 molecular-size OOD split 上报告 Graph-LLM 结果；EDT-Former 在 Bemis–Murcko scaffold split 上总准确率 `68.65`，高于 official split `68.34`，并补充 macrocycle NPMMPD，说明 scaffold/化学分布 OOD 可以落到实际 hybrid 系统 | NLGift 是纯文本 LLM benchmark，没有 node permutation、same-text topology counterfactual、GNN/LLM/readout split；OOD-GraphLLM 的 chemical scaffold/size shift 仍混入 drug identity、SMILES、retrieval、cell context 和 graph encoder 变化；EDT-Former 的 scaffold/macrocycle 结果也没有 held-out topology-family、graph-size/density 或责任分离 | 在 GraphToken/IP-GLLM/HLM-G/OOD-GraphLLM/EDT-Former 上固定节点文本/特征与任务语义，交叉执行 `10 relabelings × size/scaffold/generator/transitivity/topology-family × cross-task query × key/irrelevant intervention`，并报告 PGR、prediction equality、direction accuracy、text-only/GNN-only/readout-only 对照 |
| H11 | Graph-LLM 在 scaffold/size OOD 上的收益可能主要来自 GNN encoder、target-aware readout、retrieved biomedical semantics 或 cell-context alignment；未做责任分离前不能归因于 LLM 的 topology causal use | 待验证 | OOD-GraphLLM 将 heterogeneous bond-aware GNN、target-adaptive disentanglement、pairwise NAS、cell-context alignment 和 DrugSyn-LLM/RAG 联合优化；完整模型在 scaffold/size split 上领先，w/o NAS、w/o Ctx、w/o R-IT 等删除组件会下降；EDT-Former 中去 dynamic tokens 后 PAMPA/BBBP 从 `82.34/72.48` 降至 `54.97/51.29`，Text-only 比 full MoleculeQA 低 `14.00%`，且更强 graph encoder 仅带来 minor gains，提示 connector/interface 也可能是主要瓶颈 | 组件 ablation 只证明系统级预测贡献，没有 GNN-only、LLM-only、readout-only、text shuffle、fixed-text topology pair、node relabeling 或 key/irrelevant intervention；EDT-Former 的 dynamic-token ablation 仍把 encoder、connector 和 LLM 绑定在一起，因此无法定位 LLM 的因果责任 | 在固定 atom/node features、cell text 和 prompt 的 molecular-like matched pairs 上执行 `10 relabelings × key/irrelevant edge intervention × no-RAG/component ablation × scaffold/size holdout`，比较 full、GNN-only、LLM-only、readout-only 与 shuffled graph embedding；若 readout/GNN-only 复现完整 direction accuracy，否定 H11 的 LLM 归因 |
| H12 | 基于 SMILES surprisal 的 dynamic token 能保留化学局部结构，但其 patch/token 路径可能依赖序列遍历而不是 graph invariant | 待验证 | EDT-Former 用 canonical SMILES 的 NAP surprisal 峰切分 token，并报告与 BRICS/RECAP 的 mean NMI 约 `0.484/0.401`、不同 NAP 规模间 NMI `0.86–0.94`、dynamic-token inference ablation 和 scaffold split；这些支持局部结构相关性与功能性，但没有 equivalent-SMILES、node relabeling 或 edge-order 结果 | graph encoder、SMILES-to-node mapping 和 pooling 可能部分消除顺序影响；目前没有证明任意合法 SMILES 或同构重编号都会保持相同 patch representation，也没有 fixed-text topology intervention | 对同一 molecular graph 生成 `10 relabelings × 3+ valid SMILES`，比较 patch boundary NMI、token count、representation/prediction equality；联合关键/无关 edge intervention 与 text/graph/readout-only 对照；若等价变换稳定且关键干预方向正确，支持 H12 的强版本，否则否定其 graph-invariant 解释 |
| H13 | graph-token 的 activation saliency/attention dominance 与 task-specific topology utility 可能脱钩；显著 token 不能直接视为 topology causal bottleneck | 待验证 | Zhang et al. (2026, arXiv:2606.03712) 发现 sink activation 多位于早期位置或 `[PAD]`，不稳定获得更高 attention；top-2 sink pruning 通常比随机 non-sink pruning 更不伤害 node-task 性能，logit lens 多解码为 generic/citation-domain vocabulary | 论文只测两个架构，token pruning/swap/reposition 不是 graph-level topology intervention；sink 现象仍可能由 padding、位置和预训练域造成；link prediction 结果混合，不能据此断言 topology 完全未被 LLM 使用 | 在同一 matched graph pairs 上同时记录 sink score、attention share、pruning delta 和 fixed-text key/irrelevant topology direction；加入 node relabeling/output equality、GNN-only/LLM-only/readout-only 与 solver-disabled；若 saliency 与 topology direction 无关，否定“显著 token 即结构瓶颈” |

| H14 | 结构收益具有 task-conditional 性：当节点语义足以预测标签或图结构与语义高度混杂时，显式结构增强可能冗余甚至有害；只有在 topology 是必要信息的 matched task 上，结构接口的因果收益才可识别 | 部分支持 | When Structure Doesn't Help 在 6 个 TAG、分子任务和 Davis DTI 中观察到 semantic-only、中心节点、HN-1 或 MLP 常与 ND/GNN 相当或更好；GDC 仅在压缩并重排结构相关节点序列时出现局部收益 | HN-1 仍用图连接选择邻居，MLP 可能保留序列/采样结构；8:2 random split、文本-标签相关性和同配性未被打破；没有 fixed-text topology intervention、random-label 或 topology-family OOD，因此不能推出 LLM 完全忽略拓扑 | 构造 semantic-sufficient 与 structure-essential 的 matched tasks，固定节点文本、prompt 和输出格式，只干预关键/无关 topology；比较 text-only、HN-1、MLP、GNN、readout-only 与完整 Graph-LLM 的 direction accuracy、prediction equality 和 topology-OOD |
| H15 | partial-access 与 graph-scale 是独立的 OOD 轴：random-walk summary 的估计成功不能替代 permutation consistency、fixed-text topology causality 或 topology-family generalization | 部分支持 | EstGraph 将 random-walk/MH statistics 扩展到 100--100,000-node synthetic 图和最高 2,388,953-node real-world 图；在若干 size/community/structure/ranking estimation task 上取得有用但不稳定的近似，说明大图、局部访问和 prompt budget 可以被单独评估 | SRW/MH、coverage、seed、degree bias、Chapman estimator 和 hand-crafted prompt 可能解释主要结果；没有同图 relabeling/output equality、degree-matched topology pairs、fixed-text key/irrelevant intervention、text shuffle 或组件责任分离 | 在相同文本和输出格式的 matched topology pairs 上交叉控制 full graph、MH/SRW、coverage、burn-in、seed diversity 与 degree-only summary；联合测 `10 relabelings × edge orders`、decode/output equality、key/irrelevant direction、scale/topology-family holdout 和 readout/GNN/LLM split |

状态只能使用：`构想`、`待验证`、`部分支持`、`部分反驳`、`已支持`、`已否定`、`已合并`。

### 2026-08-18 状态更新说明

- H1：维持“部分支持”。HLM-G 是一个结构化 hierarchical encoder 的正面边界案例：它在 node relabeling 下稳定，但没有 topology counterfactual 或 LLM causal-use 证据，因此不能推翻“结构输入/任务增益不等于 LLM 因果使用”。
- H2：维持“部分支持”。HLM-G 的低 permutation drop 说明专用 local/global bottleneck 可以缓解 node-index shortcut；但 BERT、Llama-3、GraphWiz 的对照仍显示普通图文本模型会受 node position 影响，且 HLM-G 尚未迁移到 GNN-LLM/GTokenLLM。
- H5：维持“待验证”，新增正面设计证据。HLM-G 测量了 permutation robustness，却没有测同一模型的 topology counterfactual，进一步说明两类能力必须分开报告，不能由前者推出后者。

### 2026-08-19 状态更新说明

- H1：维持“部分支持”。LLM-GCE 使用 LLM 生成 TP/CTP 并在五个分子数据集上改善 GCE 的 feasible validity/proximity，但最终 counterfactual 是否有效由 GT-GNN 的 prediction loss 和 RDKit feasibility 决定；因此它补充支持“LLM-assisted graph system 的增益不等于 LLM 因果使用拓扑”。
- H6：维持“待验证”，但证据边界更清楚。与 IP-GLLM 的 latent-space soft intervention 不同，LLM-GCE 是 graph-level counterfactual explainer；它证明了可用的 GNN 反事实搜索接口，却没有证明 LLM 是拓扑干预的因果执行者。
- H8：新增并标记“待验证”。LLM-GCE 的 direct-LLM 失败、`q_G ⊕ z_G` 拼接和 GT-GNN feedback 提示语义生成、GNN oracle 与 graph decoder 的责任必须分离评估。

### 2026-08-23 状态更新说明

- H1：维持“部分支持”。NLGift 在纯文本 LLM 上显示 semantic/numeric 的有限迁移不能外推到 cross-task、graph-size 或 synthetic-to-real transfer；这补充了“任务增益和表面泛化不等于因果结构使用”的边界证据。
- H2：维持“部分支持”。NLGift 的 graph size shift 明显比 generator/transitivity shift 更难，但它没有 node relabeling 或 isomorphic consistency，因此不能把 size/generalization gap 直接解释为 node-index shortcut。
- H5/H6：维持“待验证”。NLGift 提供可复用的 PGR、size/generator/transitivity 与 cross-task 轴，但没有 fixed-text topology intervention 或 GNN/LLM responsibility split。
- H10：新增并标记“待验证”，把 topology OOD 拆为 semantic、size、generator/transitivity、cross-task 和 real-world 五条互不等价的迁移轴。

## 证据账本

| 日期 | Paper ID | 支持或挑战的假设 | 证据层次 | 关键证据 | 局限 |
|---|---|---|---|---|---|
| 2026-08-15 | arXiv:2605.03514 | 支持 H1；提出并待验证 H4 | 结构可解码、结构被使用（仅间接） | GTEval 在 6 个 GTokenLLM 上显示 Rephrasing、Relabeling、Reversing、Randomizing 下性能显著不稳定；LLaGA 的 GT/GE 可被 GCN 用于 link prediction，PRBCD 下 GraphGPT/TEA-GLM 几乎不变；attention 不能单独证明因果利用 | 未测 node permutation/isomorphism、保持语义的 graph counterfactual、text shuffle、held-out topology OOD；PRBCD 是攻击而非严格因果干预 |
| 2026-08-16 | arXiv:2511.10234 | 支持 H1/H2；提出并待验证 H5 | 结构被使用（间接）、结构可泛化（弱） | 同一图 10 次 node relabeling、edge-list/adjacency-list/matrix、edge ordering 与四种 syntax 产生可测 accuracy shift 和 output variability；G1 对部分序列化规律更脆弱；12 个 spectral tasks 上 base/fine-tuned models 均未可靠泛化 | 未测 representation probe、same-text topology counterfactual、text-only/graph-only、held-out topology family；部分 computational structure 变换会改变计算路径，不是纯粹的 causal structure ablation；研究对象是纯序列化 LLM，不是 GNN-LLM hybrid |
| 2026-08-17 | doi:10.1007/978-981-92-3520-9_19 | 补充 H1；为 H4/H6 提供待验证方法 | 结构被使用（间接）、结构可泛化（间接） | IP-GLLM 用局部 GIN tokenizer、REC-Context 和 learnable interventional prompts 近似因果筛选；在 8 个 synthetic/real-world benchmark、confounding sweep、transfer 和组件消融上报告更稳健的 OOD/transfer；prompt removal 平均下降 4.6 points，zero-shot SPMotif 达 63.6% | 未测 node permutation/isomorphism、same-text topology counterfactual、text shuffle、GNN-only/LLM-only；latent prompt intervention 不是 graph-level do intervention；attention 与 OOD gain 仍可能由 GNN/tokenizer 完成，论文对象当前仅为 graph classification |
| 2026-08-18 | arXiv:2410.22372 | 补充 H1/H2；提出并待验证 H7；为 H5/H6 提供对照 | 结构可解码、结构被使用（间接）、结构可泛化（弱） | HLM-G 将 1-hop structure annotation 经 local intra-node attention、结构/特征 pooling 和 global inter-node attention 送入 query readout；合成任务高 accuracy，10 次 node permutation 的 accuracy drop 为 `0.0/0.0/6.1/0.8/0.1/3.0/10.2`；pooling、local embedding similarity、Recall@k 和 cross-prompt/node-token OOD 提供多层间接证据 | 主合成任务是同一随机图生成器的 80/10/10 random split；没有 same-text topology counterfactual、text shuffle、逐样本 isomorphism consistency、held-out topology family、GNN-LLM permutation 对照；主模型是专用 hierarchical encoder/classifier，attention/accuracy 不能证明 LLM causal use | 将 HLM-G 的 10-way relabeling protocol 与 fixed-node-text topology counterfactual、edge-order/syntax、GNN-only/LLM-only、encoder/readout-only 和 topology-family OOD 组合，检验 H7 与 H6 |
| 2026-08-19 | doi:10.18653/v1/2024.findings-emnlp.415 | 补充 H1/H6；提出并待验证 H8 | 结构输入、结构可解码（combined system）、结构反事实（任务级 GCE） | LLM-GCE 把 `G=(X,A,E)` 与 SMILES/TP/CTP 输入 GT-GNN、BERT text encoder 和 CA；`L_dist + L_pred - L_KL` 以及 dynamic feedback 生成更可行、更近的 counterfactual；直接 GPT-4 生成 valid counterfactual 的 validity 为 0；GT-GNN test accuracy 为 AIDS/Mutagenicity/BBBP/ClinTox/Tox21=`0.994/0.762/0.850/0.783/0.974` | 没有 node permutation/isomorphism、same-text topology intervention、text shuffle、q_G removal/shuffle、LLM-only/GNN-only 责任分离或 held-out topology-family；最终翻转由 GT-GNN oracle 定义，可能主要是 GNN/decoder 在完成结构计算；仅为 LLM-assisted GNN explanation，不是 Graph-LLM final prediction | 用 matched fixed-node-text topology pairs 交叉测试 LLM-GCE、q_G-shuffle、text-only、GNN-only、CA/readout-only 与 IP-GLLM/GraphToken，报告 direction accuracy、feasible validity、permutation consistency 和 topology OOD |
| 2026-08-22 | doi:10.18653/v1/2025.findings-naacl.110 | 补充 H1/H2；为 H4/H5 提供待验证证据；提出并待验证 H9 | 结构输入、结构被使用（任务级间接）、shortcut control | CausalGraph2LLM 将同一 causal DAG 用 JSON、adjacency、adjacency-matrix、GraphML、Graphviz、single-node、multi-node 以及 visual encoding 输入 6 个 LLM；报告最高约 60% encoding variation，BFS/BFS-R 顺序影响 source/sink，random-vs-semantic labels 与 anti-commonsense DAG 暴露 pretraining bias；`do(X=x)` downstream intervention 对 encoding 敏感，adjacency-matrix 在 GPT-4 上接近 0.50 random baseline | 没有 node permutation/isomorphism、same-text topology pair、text shuffle、GNN/LLM/readout responsibility split 或 held-out topology-family；zero-shot prompt benchmark 的 intervention accuracy 不是对 Graph-LLM 内部拓扑表征的 causal mediation | 将 7-way encoding/order、random-vs-semantic label 和 `do(X=x)` direction task 移植到 GraphToken/IP-GLLM/HLM-G fixed-text matched topology pairs，联合报告 permutation flip、key/irrelevant direction accuracy、text shuffle 和 encoder/readout-only 对照 |
| 2026-08-23 | doi:10.18653/v1/2024.findings-emnlp.127 | 补充 H1/H2/H5/H6；提出并待验证 H10 | 结构输入、结构可泛化（弱）、shortcut control | NLGift 在 37,000 个问题上评估 semantic、numeric、structural、reasoning、real-world 五类转移；graph size 仅 2/8 设置达到 Strong Recovery，跨 reasoning task 无 Strong Recovery，synthetic tuning 对 real-world tasks 几乎无普遍收益；task composition 与 Dolma keyword frequency 暴露文本/任务捷径 | 没有 node permutation/isomorphism、same-text topology counterfactual、edge-order/text shuffle、GNN-only/LLM-only/readout-only 或 held-out topology-family；real-world 设置部分不提供显式 graph context，size shift 混入 token length 与任务难度 | 将 NLGift 的 PGR、graph-size、generator/transitivity、cross-task 和 synthetic-to-real 轴迁移到 GraphToken/IP-GLLM/HLM-G fixed-text matched pairs，并与 10-way relabeling、encoding/order、key/irrelevant topology intervention 及责任分离联合 |
| 2026-08-25 | arXiv:2605.30247 | 补充 H1/H10；提出并待验证 H11 | 结构输入、结构可泛化（间接）、shortcut control（部分） | OOD-GraphLLM 将 molecular graph 经 heterogeneous bond-aware GNN、target-adaptive disentanglement、pairwise NAS 和 projector 注入 DrugSyn-LLM；在四个 DrugComb score 的 scaffold/size OOD split 上完整模型取得领先；w/o NAS、w/o Ctx、w/o R-IT、w/o Attn、w/o Sep、w/o Decor 均有下降；三种 prompt wording 结果较稳定 | 没有 node relabeling/isomorphism、same-text topology intervention、text shuffle、random-label、GNN-only/LLM-only/readout-only 或 held-out topology-family；scaffold/size shift 混入 drug identity、SMILES、RAG、cell context 和 encoder 变化，attention/case study 不能证明 LLM causal use | 将 scaffold/size split 与 fixed-text molecular-like matched topology pairs 联合，加入 `10 relabelings × key/irrelevant edge intervention × no-RAG × GNN-only/LLM-only/readout-only`，报告 prediction equality、permutation flip rate、direction accuracy、AUC/MAE 与 PGR |
| 2026-08-26 | arXiv:2602.02742 | 补充 H1/H2/H10/H11；提出并待验证 H12 | 结构输入、结构可解码（间接）、结构被使用（间接）、结构可泛化（弱）、shortcut control（部分） | EDT-Former 以 frozen graph encoder node embeddings 为输入，用 SMILES surprisal peaks 形成 dynamic substructure tokens；MoleculeQA 的 Text/Graph/3D 为 `68.34`、Text-only 为 `58.77`；去 dynamic tokens 后 PAMPA/BBBP 从 `82.34/72.48` 降至 `54.97/51.29`；Entropy/BRICS/Random/None patching 与 graph-encoder、LLM-freezing、clean-set 和 scaffold split ablation 提供多层系统级证据 | 没有 node relabeling/isomorphism、equivalent-SMILES/edge-order、same-text topology intervention、text shuffle、GNN-only/LLM-only/readout-only 或 key/irrelevant edge control；dynamic token 与 attention 不能单独证明 LLM causal topology use，scaffold split 也不是 topology-family OOD；patch boundary 依赖 SMILES traversal | 对 EDT-Former/OOD-GraphLLM 做 `10 relabelings × 3+ valid SMILES × key/irrelevant topology intervention × no-RAG × GNN-only/LLM-only/readout-only`，报告 patch NMI、token count、prediction equality、permutation flip rate、direction accuracy、scaffold/size PGR 与 text/graph shuffle 对照 |
| 2026-08-27 | doi:10.1186/s13321-025-01079-0 | 补充 H1/H2/H5/H9/H12 | 结构输入、结构可解码（间接）、结构可泛化（representation OOD）、shortcut control（部分） | AMORE 在 ChEBI-20 与 C9H12N2O isomers 上用 5 类 identity-preserving SMILES transformation 测 12 个 ChemLM；ChEBI-20 上 explicit-hydrogen 与 random atom-order retrieval 明显困难，Isomers 上 hydrogen Acc@1 几乎为 0；layer-wise hidden retrieval、Levenshtein/token length、ROUGE/METEOR 对照和 MoleculeNet augmented-test 共同显示表示格式会影响模型 | 研究对象是纯 sequence ChemLM，不是 GNN-LLM；没有 Graph-LLM connector、node relabeling 全集、fixed-text key/irrelevant edge intervention、text shuffle、GNN-only/LLM-only/readout-only 或 topology-family split；augmentation type 混入 token/长度/预训练分布差异，embedding retrieval 不等于 final causal use | 将 AMORE 的 `Acc@1/Acc@5/MRR + layer-wise + heavy-atom/ring stratification` 接入 EDT-Former/OOD-GraphLLM 的 `10 relabelings × 3+ valid SMILES × key/irrelevant topology intervention`，同时报告 patch NMI、token count、prediction equality、direction accuracy、no-RAG 与 GNN/LLM/readout responsibility split |
| 2026-08-29 | arXiv:2606.09484 | 补充 H1/H2；部分支持 H5 | 结构被使用（间接）、shortcut control | 在 400 个非同构图对上三种 LLM 的 ordinary isomorphism accuracy 接近满分；但同一图随机重标记生成 400 个 isomorphic pairs 后，OpenAI 为 0-18%、Gemini 为 0-0.2%、Llama 为 2-39.2%，且 edge list/index/matrix 之间存在明显差异 | 研究对象是纯序列化 LLM，不是 GNN-LLM；没有 hidden-state probe、fixed-text topology intervention、text shuffle、GNN/LLM/readout split、held-out topology family，也未控制 girth、diameter、symmetry、automorphism 或 tokenization；permutation failure 是 shortcut/robustness 证据，不是 causal topology proof | 将 ordinary task accuracy 与 `10 relabelings × encoding/order variants × prediction equality/flip rate` 接入 GraphToken/IP-GLLM/HLM-G/EDT-Former/OOD-GraphLLM，并进一步加入 fixed-text key/irrelevant intervention、GNN-only/LLM-only/readout-only 和 topology-family OOD |
| 2026-08-29 | arXiv:2603.11099 | 支持 H1/H2/H5；推进 H3 | 结构输入、结构可解码、结构可泛化（弱）、shortcut control（边界对照） | GraphTokenizer 用 `labeled graph -> frequency-guided reversible serialization -> BPE -> BERT/GTE`；Feuler/FCPP 同时追求 reversibility 与 determinism，BPE 在 ZINC 上约 10x 压缩序列；14 个 graph-level benchmark 上 GT+GTE 多数任务领先，COIL-DEL 为 89.6，textualized GraphGPT/LLAGA 仅 5.6/12.5 | 没有 empirical node-relabeling/isomorphism consistency、fixed-text topology intervention、text shuffle、hidden-state mediation 或 GNN/LLM/readout split；continuous features、node/edge-level addressability 和大图 context window 是限制；纯结构任务优势不能单独证明 Transformer/LLM causal topology use | 在同一 Transformer 上加入 `10 relabelings × 4 edge orders × 3 starts/directions`、decode isomorphism、key/irrelevant topology intervention 和 held-out topology-family；比较 reversible discrete token、raw serialization、continuous projector、GNN-only/readout-only 与 text-only |
| 2026-08-30 | arXiv:2605.22471 | 支持 H1/H2/H5；强化 H3 | 结构输入、结构可解码、结构被使用（任务级间接）、结构可泛化（弱） | Lost in Tokenization 形式化 graph tokenization 为计算模型的一部分；证明 adjacency 对 closed-walk/connectivity 有有限深度瓶颈，random-walk 对 planarity 有不可辨识性，truncated spectral/adjacency 存在信息或带宽损失，且 real-world task 对 local/global/diffusion views 有不同偏好；combined view 在部分任务更强 | 论文只研究标准 Transformer Encoder、GIN、DeepSet，不是 Graph-LLM；理论边界依赖复杂度分离假设，实验是 standard split 且没有 node relabeling final-output equality、fixed-text topology intervention、text shuffle 或 GNN/LLM/readout responsibility split；combined gain 不能证明 LLM causal topology use | 将 I1 从单一 reversible sequence 审计扩展为 task-conditioned local/global/diffusion view 对照；在同一 Transformer 上联合测 `decode fidelity + permutation equality + key/irrelevant intervention + topology-family OOD`，并比较 single-view、combined、GNN-only/readout-only |
| 2026-08-30 | arXiv:2506.07334 | 补充 H1/H2/H5；为 H6/I1 提供结构路由对照 | 结构被使用（间接）、结构可泛化（弱）、shortcut control（部分） | Graph-KV 在 LLM-side KV-cache 中对 source→target 节点施加图邻接 attention mask，并用 shared positional embedding 缓解 chunk-order bias；Top-3 在多跳 RAG、Cora/Pubmed topic classification 和大邻居 stress test 上优于顺序/块式基线，且对 chunk placement/distractor 有一定稳健性 | 没有 node permutation/isomorphism、fixed-text key/irrelevant topology intervention、text shuffle、GNN/LLM/readout responsibility split 或 topology-family OOD；RAG 边由 semantic retriever 启发式构造，主实验 one-hop，Graph-KV-FT backbone 与 no direct fine-tuning 混入；mask/共享 PE 的收益不能证明 discrete token 或 LLM causal topology use | 将 Graph-KV-style mask-only 作为 I1 的 matched baseline，与 reversible discrete/BPE、continuous projector、raw serialization 一起执行等价变换 equality、fixed-text topology intervention、责任拆分与 topology-family OOD |
| 2026-08-31 | arXiv:2608.27142 | 补充 H1/H2/H5/H10；强化 I1 | 结构可解码、结构被使用（间接）、结构可泛化（表面/规模轴）、shortcut control（部分） | GRAIN 在 8 个同底层图的命名/叙事 views 上训练和评估，Qwen3-Base SFT 平均 95.76%，OOD gap 7.80%，exact graph recovery 97.28%；同时明确由 graph-tool 外部 solver 负责主要算法计算。GraphTokenizer 官方 release 静态代码显示默认 `start_node=0`、disconnected component 重置 `sub_start_node=0`，并以 neighbor ID 作为频率相同情况下的稳定 tie-break | GRAIN 没有 fixed-text topology intervention、arbitrary node relabeling/output equality、text shuffle、GNN/LLM/readout split 或 held-out topology family；known mapping/canonical edge Jaccard 不是 graph-isomorphism equality；GraphTokenizer 本地环境缺少运行依赖，本轮仅完成静态审计 | 将 GRAIN-style exact graph IR/parser-oracle baseline 与 GraphTokenizer 的 `10 relabelings × 4 edge orders × 3 starts/directions`、fixed-text key/irrelevant intervention、责任拆分和 topology-family OOD 放入 I1 comparison matrix |
| 2026-09-01 | arXiv:2606.03712 | 补充 H1/H5；提出并待验证 H13；强化 I1 | 结构被使用（token-level 间接）、结构反事实（token-level）、shortcut control（部分） | 两种 Graph-LLM 上 sink activation 倾向出现在早期 graph-token positions；LLaGA top-2 sink 常为 `[PAD]` 且 center token 从未成为 sink；sink 未稳定获得更高 attention，top-2 sink pruning 通常不如随机 non-sink pruning 造成 node-task 性能下降；TEA-GLM logit lens 多为 generic/citation-domain vocabulary | 没有 node relabeling/isomorphism、fixed-text key/irrelevant topology pair、text shuffle、GNN/LLM/readout responsibility split 或 held-out topology-family；token position intervention 不能替代 graph-level topology intervention；link prediction 结果混合；只覆盖两个架构 | 将 sink score/attention/pruning 作为 I1 pre-intervention diagnostic，与 `10 relabelings × 4 edge orders × 3 starts/directions`、decode isomorphism、fixed-text topology direction、GNN-only/LLM-only/readout-only、solver-disabled 和 topology-family OOD 联合验证 |
| 2026-09-02 | arXiv:2511.16767 | 强化 H1；对 H3 施加反驳证据；提出并待验证 H14；强化 I1 的失败条件 | 结构输入、结构被使用（任务级间接）、shortcut control（部分）、结构必要性反证 | ND 的 Laplacian/结构模板在多项 TAG 任务上被 HN-1/CO 匹配或超过；GraphToken 的 MLP 平均 node classification 高于 GCN/GAT/GIN；BACE/HIV 的 MLP 与 GNN 接近；Davis 中 sequence encoder MSE `1.1103±0.0114` 不差于 structure encoder `1.1349±0.0100`；GDC 的中心化序列在部分设置有改善，说明结构收益可能依赖任务和序列接口 | HN-1 仍按图选择 k-hop neighbors，MLP 仍可能保留序列/采样结构；主要数据使用 8:2 random split，文本-标签相关性、同配性、容量/预训练差异和 DTI 模态差异未完全控制；没有 node relabeling、fixed-text key/irrelevant topology intervention、text shuffle、GNN/LLM/readout split 或 topology-family OOD | 在 semantic-sufficient/structure-essential matched tasks 上先做 text-only、HN-1、MLP、GNN、readout-only 分层，再把 I1 的 decode equality、`10 relabelings × 4 edge orders × 3 starts/directions`、fixed-text topology direction 和 topology-family OOD 接入；若结构必需子集仍被语义-only/readout-only 复现，则反驳 I1 的方法增益 |
| 2026-09-03 | doi:10.18653/v1/2026.acl-long.1846 | 补充 H1/H5/H10；提出并待验证 H15；强化 I1 | 结构输入、结构被使用（任务级间接）、结构可泛化（scale/access 间接）、shortcut control（部分） | EstGraph 用 random-walk statistics 将 LLM graph evaluation 扩展到 partial-access large graphs；synthetic 覆盖 100--100,000 nodes，SNAP real-world 图最高 2,388,953 nodes；节点/边估计在不少 synthetic 配置 median relative error 低于 20%，Grid 结构识别为 100%，o3 的 PageRank Precision@20 为 81.0% | 没有 node permutation/isomorphism、same-text topology intervention、text shuffle、degree-only/walk-stat shuffle、GNN/LLM/readout split 或 held-out topology-family；SRW/MH、coverage、seed、degree distribution、Chapman estimator、prompt statistics 和 LLM API variance 混合，PageRank 与 visit frequency 也存在直接统计相关 | 将 EstGraph 的 MH/SRW、coverage、burn-in、seed diversity、degree-only 和 median-error/PGR 迁移为 I1 的 access-aware OOD slice；保留 structure-essential gate，并在小图加入 `10 relabelings × edge orders × starts/directions`、decode/BPE/output equality、fixed-text key/irrelevant direction、GNN-only/readout-only 和 topology-family holdout |

证据层次使用：`结构输入`、`结构可解码`、`结构被使用`、`结构可泛化`。

## 未解决问题

1. Q1：现有代表性 GNN-LLM 方法分别提供了哪一层结构证据？
2. Q2：哪些工作真正使用了 permutation、counterfactual intervention 或 topology OOD 测试；这些证据是否能迁移到 GTokenLLM？
3. Q3：如何构造同文本、不同关键拓扑的反事实图对，排除语义捷径？
4. Q4：怎样检测 GNN 已完成答案计算而 LLM 仅承担语言解码？
5. Q5：如何把 instruction-level robustness 与 permutation、text shortcut control、topology-OOD 组合成一个能区分“结构可解码”和“结构被因果使用”的最小评测？
6. Q6：如何区分等价序列化鲁棒性、计算路径鲁棒性和真正的 topology-OOD 泛化？
7. Q7：LLM-assisted GNN explainer 的 graph-level counterfactual 能否迁移为 Graph-LLM 的 fixed-text topology intervention，并分离 LLM、GNN oracle 和 graph decoder 的责任？
8. Q8：在 OOD-GraphLLM 这类 molecular Graph-LLM 中，scaffold/size OOD 增益在移除 RAG、固定 node features/text 并比较 GNN-only/readout-only 后，是否仍由 LLM 贡献？
9. Q9：基于 canonical/SMILES traversal 的 dynamic graph tokens 是否在合法等价序列化和 node relabeling 下保持相同结构证据，还是把序列预测难度误当成图不变量？
10. Q10：GraphTokenizer 声称的 deterministic reversible serialization 在重复 node/edge labels、起点/方向、edge order 和 disconnected components 下是否真正产生等价 token sequence；BPE 是否会重新引入 serialization-order shortcut？
11. Q11：在 GraphTokenizer 的 reversible discrete interface 中，task-conditioned complementary views 能否在有限深度和 token budget 下同时改善 local/global topology 任务，并保持等价序列化稳定与可定位的因果干预？
12. Q12：当结构恢复 reward 只约束 LLM 输出的显式 graph IR、而拓扑算法由 external solver 执行时，如何把 parser-level robustness 与 Graph-LLM 的 hidden/output causal topology use 区分开？
13. Q13：graph-token activation sink、attention share 与 pruning utility 是否只是 padding/位置/预训练域现象；它们能否预测固定文本关键/无关 topology intervention 的方向，还是与真正的 topology causal responsibility 脱钩？
14. Q14：在 semantic-sufficient 与 structure-essential 的 matched tasks 中，结构接口的收益是否仍能通过 fixed-text key/irrelevant topology intervention、置换一致性和 responsibility split 被识别；还是会被文本、邻居采样、序列顺序或 readout 复现？
15. Q15：当 Graph-LLM 只能通过 random walk 或 sampled subgraph 访问大图时，如何把 coverage、seed、degree bias 与真正的 topology-family OOD、permutation consistency 和 causal topology use 分开？

## 下一轮检索队列

1. 以 LLM-GCE 的 `GT-GNN oracle + graph distance + prediction flip + feasibility` 为基线，构造固定节点文本/特征的 causal-ring/star-confounder matched topology pairs；首先做 q_G removal/shuffle、text shuffle、CTP random、GNN-only/readout-only 对照。
2. 继续寻找直接测量 GNN-LLM/GTokenLLM 的 node permutation 或 isomorphic graph consistency：`GTokenLLM permutation invariance node relabeling`、`GraphGPT LLaGA isomorphic graph consistency`、`GraphMind node edge order robustness`。
3. 将 `10 relabelings × edge-order/syntax variants × same-text topology counterfactual × text shuffle` 与 graph-only/text-only、GNN-only、LLM-only、held-out topology family 和 graph-size extrapolation 组合；若仍找不到现成联合评测，下一次优先精读能同时分离 graph decoder/GNN oracle/LLM 的工作。
4. 针对 EDT-Former，先复现 `Entropy/BRICS/Random/None patching` 和 `full/no/random dynamic tokens`，再在同一分子上加入 `10 relabelings × 3+ valid SMILES × fixed atom features/text`，测 patch NMI、token count、prediction equality 和关键/无关 edge direction。

5. 将 `When Graph Tokens Sink` 的 sink-dimension、attention share、pruning/reposition/swap 和 logit-lens 诊断迁移到 I1；先做 token-level saliency 与 utility 对照，再叠加 fixed-text topology intervention、责任拆分和 topology-family OOD，避免把 activation outlier 当作 causal topology proof。
6. 新增 semantic-sufficiency gate：先在同长度、同文本、同 prompt 的 semantic-sufficient/structure-essential matched tasks 上比较 text-only、HN-1、MLP、GNN 和 readout-only；只有结构必需子集出现独立 topology direction signal 后，再运行 GraphTokenizer 的 `10 relabelings × 4 edge orders × 3 starts/directions` 与 decode/BPE equality audit。
7. 将 EstGraph 的 partial-access/scale 轴迁移到 I1：在 full graph、MH/SRW、不同 coverage/burn-in/seed 和 degree-only summary 下，测 size/topology-family OOD；再与 fixed-text intervention、等价输出和 responsibility split 交叉，避免把 sampling statistics 当作 topology causality。

本轮优先级调整：下一次先把 LLM-GCE 的 graph-level counterfactual protocol 迁移到 fixed-node-text matched pairs，核验 q_G removal/shuffle、text shuffle 和 direct-LLM 是否改变关键/无关 topology 的 direction accuracy；随后继续 IP-GLLM 的 causal-ring/star-confounder matched pairs，并把 HLM-G、GraphToken 的 encoder/readout-only 对照加入统一 protocol。检索词：`LLM-GCE q_G ablation counterfactual`、`Graph-LLM fixed text topology intervention`、`GNN oracle versus LLM graph reasoning`、`Graph-LLM node permutation isomorphism consistency`。

### 2026-09-02 状态更新说明（When Structure Doesn't Help）

- H1：维持“部分支持”并增强反面证据。When Structure Doesn't Help 在语义丰富的 TAG、分子和 DTI 设置中显示 semantic-only、中心节点、HN-1 或 MLP 常能匹配/超过结构-aware ND/GNN；这支持“标准任务增益不等于 LLM causal topology use”，但不能证明 LLM 完全不读拓扑。
- H3：维持“待验证”，新增“结构必要性反驳”约束。显式 Laplacian/消息传递在本论文任务上并非稳定增益，GDC 的局部改善又说明问题可能在于 task-conditioned sequence/interface，而非结构信息绝对无用；I1 必须先通过 structure-essential matched task gate，不能只在语义丰富的 random split 上宣称结构瓶颈有效。
- H5：维持“部分支持”。语义充分性、序列选择、置换一致性与 topology causal use 继续表现为不同轴；今天的 MLP/HN-1 结果不能替代 fixed-text key/irrelevant intervention。
- H10/H11：维持“待验证”。多数据集、模型规模和 DTI 对照增加了任务/模态压力证据，但 8:2 random split、预训练差异、邻居采样和缺失 responsibility split 仍阻止 topology-OOD 或 LLM 归因结论。
- H14：新增并标记“部分支持”。结构收益必须按 semantic-sufficient 与 structure-essential 任务条件化；最小验证是同文本、同 prompt、同长度的 matched topology pairs，配合 random-label/text shuffle、key/irrelevant edge intervention、GNN/MLP/readout-only 和 held-out topology family。

2026-09-02 本轮优先级调整：先执行 semantic-sufficiency gate，构造可由中心文本预测的对照任务与由 shortest-path/connectivity/cycle 决定标签的 structure-essential 任务，保持文本、prompt、长度和输出格式匹配；只在后者对 text-only、HN-1、MLP、GNN/readout-only 形成独立差异后，再运行 GraphTokenizer 的 serialization/BPE/decode equality audit。若结构必需子集仍由 semantic-only/readout-only 复现，I1 降级为诊断协议；若出现方向正确的 topology response，再继续 responsibility split 与 topology-family OOD。检索词：`structure-essential graph task fixed text LLM`、`semantic sufficiency graph LLM topology intervention`、`Graph-LLM readout-only MLP GNN comparison`、`GraphTokenizer BPE permutation equality`。

### 2026-08-31 状态更新说明（GRAIN）

- H1：维持“部分支持”。GRAIN 的 exact graph IR recovery 与最终任务分数可以由语义解析、实体对齐和 external deterministic solver 共同产生，不能单独证明 LLM 内部完成 topology causal computation。
- H2：维持“部分支持”。GRAIN 覆盖已知 mapping 下的命名/叙事变化，但不是 arbitrary node relabeling；GraphTokenizer 官方 Feuler 源码中的默认起点、component reset 和 neighbor-ID tie-break 使 ID 依赖风险成为可操作的静态审计对象。
- H4：维持“待验证”。GRAIN 联合了 surface-form invariance 与 exact recovery，但没有 fixed-text topology counterfactual、text shuffle 和完整 responsibility split，尚未闭合从 parser robustness 到 causal topology use 的证据链。
- H5：维持“部分支持”。表面/叙事不变性、exact graph recovery 和 topology causal use 是不同轴；GRAIN 的 known-mapping Jaccard 不能替代逐样本 isomorphism/output equality。
- H10：维持“待验证”。GRAIN 增加了 naming、narrative、规模和 OOD 轴，但图主要是 connected undirected Erdős–Rényi，尚无 held-out topology-family、generator/transitivity 和 directed/weighted topology 的完整泛化证据。
- H11：维持“待验证”。本轮新增 parser、graph IR、external solver 与 LLM answer head 的责任边界，需用 oracle/readout-only 和 solver-disabled 对照验证。

2026-08-31 本轮优先级调整：GraphTokenizer 静态审计已定位 `start_node=0`、`sub_start_node=0` 和 neighbor-ID tie-break；下一轮若能提供隔离环境依赖，运行官方 serializer/decode 在重复 labels、disconnected components、`10 relabelings × 4 edge orders × 3 starts/directions` 下的 equality。将 GRAIN-style exact graph recovery/parser-oracle baseline 加入 I1，并在相同 matched pairs 上联合执行 fixed-text key/irrelevant topology intervention、text shuffle、GNN-only/LLM-only/readout-only、solver-disabled 和 held-out topology-family；若只保持 surface-form robustness 或 parser recovery，而不产生可定位且方向正确的 topology intervention，I1 只保留为评测协议。检索词：`GRAIN exact graph recovery parser oracle Graph-LLM`、`GraphTokenizer Feuler start_node permutation`、`external solver Graph-LLM causal topology`、`fixed text graph IR intervention responsibility split`。

2026-08-22 本轮优先级调整：CausalGraph2LLM 提供了可复用的 7-way encoding、BFS/BFS-R ordering、random-vs-semantic label 和 downstream `do(X=x)` 诊断轴，但仍不是 Graph-LLM causal-use 证据。下一轮应优先寻找或构造能把这些轴与 fixed-text matched topology pairs 交叉的 GNN-LLM 工作；检索词：`Graph-LLM causal query encoding intervention`、`fixed text topology counterfactual Graph-LLM`、`GraphToken permutation encoding robustness`、`CausalGraph2LLM Graph-LLM transfer`。若没有直接论文，则继续审计 IP-GLLM、GraphToken、HLM-G 的 encoder/readout-only 与 LLM-only 对照。

2026-08-23 本轮优先级调整：NLGift 将 OOD 从单一 topology split 拆成 semantic、graph-size、generator/transitivity、cross-task 和 synthetic-to-real 五轴，但仍是纯文本 LLM 边界对照。下一轮应把 NLGift 的 `PGR/Strong Recovery` 和 size/task transfer 迁移到 GraphToken/IP-GLLM/HLM-G 的固定节点文本 matched pairs，并同时加入 `10 relabelings × encoding/order × key/irrelevant topology intervention × text shuffle`；优先核验 encoder/readout-only、GNN-only、LLM-only 和 real-world explicit-graph context 对照。检索词：`Graph-LLM topology OOD graph size extrapolation`、`GraphToken cross-task generalization`、`GNN-LLM synthetic to real graph reasoning`、`fixed text topology counterfactual Graph-LLM`。

2026-08-25 本轮优先级调整：OOD-GraphLLM 将 scaffold/size OOD 从纯文本 benchmark 推进到 molecular Graph-LLM，但其完整模型仍混合 GNN、NAS、cell-context、SMILES、RAG 与 LLM。下一轮优先构造固定 atom/node features、cell text 和输出格式的 molecular-like matched topology pairs，加入 `10 relabelings × key/irrelevant edge intervention × no-RAG × GNN-only/LLM-only/readout-only`，验证 H10/H11；同时复核 OOD-GraphLLM 官方代码是否能复现实验 split。检索词：`OOD-GraphLLM GNN-only ablation`、`molecular Graph-LLM fixed text topology counterfactual`、`Graph-LLM scaffold split responsibility`、`Graph-LLM readout-only topology intervention`。

2026-08-26 本轮优先级调整：EDT-Former 将分子 Graph-LLM 的结构接口推进到 dynamic substructure tokens，并提供 Text/Graph/3D、dynamic-token removal/randomization、graph encoder、LLM-freezing、clean-set 和 Bemis–Murcko scaffold controls，但其 patch 边界依赖 canonical SMILES，仍没有 equivalent-SMILES、node permutation、fixed-text topology intervention 或 GNN/LLM/readout responsibility split。下一轮优先复核 EDT-Former 和 OOD-GraphLLM 的公开代码/数据划分，构造固定 atom/node features、prompt 与输出格式的 molecular-like matched pairs；联合运行 `10 relabelings × 3+ valid SMILES × key/irrelevant edge intervention × no-RAG × GNN-only/LLM-only/readout-only`，并比较 patch NMI、prediction equality、direction accuracy、scaffold/size PGR。检索词：`EDT-Former code permutation SMILES`、`molecular Graph-LLM equivalent SMILES robustness`、`Graph-LLM fixed atom features topology intervention`、`OOD-GraphLLM readout-only ablation`。

## 已否定或暂停方向

暂无。后续不得删除旧条目；说明否定证据和日期后保留。

### 2026-08-25 状态更新说明

- H1：维持“部分支持”。OOD-GraphLLM 的完整 Graph-LLM 在 scaffold/size OOD 上优于 DNN、GNN 和其他 LLM baseline，但 graph encoder、RAG、cell context、SMILES 与 LLM 是联合系统；该结果支持“结构输入与系统级 OOD 增益”，不证明 LLM 单独因果使用 topology。
- H5/H6：维持“待验证”。论文没有 node relabeling/isomorphism、same-text topology counterfactual、text shuffle 或 key/irrelevant topology intervention；chemical scaffold/size shift 不能替代这些控制。
- H10：维持“待验证”，证据从纯文本 NLGift 扩展到实际 Graph-LLM 的 scaffold/size OOD，但仍需拆分 semantic/retrieval、GNN encoder、cell-context、molecular size 和 topology-family 的贡献。
- H11：新增并标记“待验证”。OOD-GraphLLM 的 `w/o NAS`、`w/o Ctx`、`w/o R-IT` 等消融能定位系统组件价值，却不能定位 LLM 的 topology causal responsibility；最小验证必须加入 GNN-only、LLM-only、readout-only、fixed-text topology pairs 和 direction accuracy。

### 2026-08-26 状态更新说明

- H1：维持“部分支持”。EDT-Former 在 graph/text/3D fusion、dynamic-token removal 和 scaffold split 上取得系统级增益，但 graph encoder 与 connector 先完成结构压缩，LLM causal topology use 仍未被单独验证。
- H2/H5/H9：维持原状态并新增 H12“待验证”。Entropy-Guided Patching 的结构边界由 canonical SMILES surprisal 峰决定；BRICS/RECAP NMI、NAP-size stability 和 attention 只说明序列片段与局部化学结构相关，不能推出 equivalent-SMILES 或 node permutation consistency。
- H10：维持“待验证”。MoleculeQA 的 Bemis–Murcko scaffold split `68.65 vs 68.34` 和 macrocycle evaluation 补充了 chemical-distribution OOD，但没有覆盖 graph-size、density、topology-family 或 cross-task topology generalization。
- H11：维持“待验证”，证据出现双向性。EDT-Former 的 `no dynamic tokens` 大幅下降、Text-only `14.00%` 平均下降、stronger encoder minor gain 提示 connector/interface 也可能是主要瓶颈，削弱“收益必然主要来自 encoder”的解释；但没有 GNN-only、LLM-only、readout-only 和 fixed-text intervention，仍不能把责任归给 LLM。

### 2026-08-27 状态更新说明

- H1：维持“部分支持”。AMORE 说明即使没有 GNN、只用 sequence-based ChemLM，identity-preserving SMILES 变体也会造成 hidden embedding retrieval 和 downstream metric shift；这补充了“任务分数或系统增益不等于结构因果使用”的边界证据，但不能直接外推到 Graph-LLM。
- H2：维持“部分支持”。random atom order、canonicalization、Kekulization、cycle renumbering 和 explicit hydrogen 的结果把序列/ token shortcut 从 Graph-LLM 之外的分子 ChemLM 侧具体化；但 augmentation 的长度和预训练分布混杂仍需控制。
- H5/H9：维持“待验证”。AMORE 提供了 embedding-level equivalent-representation consistency 轴，却没有 fixed-text topology intervention；因此表示稳定性、encoding/order robustness 与 causal topology use 仍必须分开报告。
- H10：维持“待验证”。augmented MoleculeNet 属于 representation OOD，不能替代 graph-size、density、topology-family 或 cross-task OOD。
- H12：维持“待验证”并强化风险证据。AMORE 使 EDT-Former 的 canonical-SMILES patch shortcut 更值得优先核验，但 AMORE 不是 EDT-Former，不能据此断言 dynamic token 已违反 graph invariance。

2026-08-27 本轮优先级调整：AMORE 提供了可直接迁移的 identity-preserving SMILES augmentation、embedding retrieval 和 layer-wise audit，但仍没有 Graph-LLM responsibility split。下一轮优先复核 EDT-Former/OOD-GraphLLM 的公开代码和 canonicalization，构造 `10 relabelings × 3+ valid SMILES × key/irrelevant fixed-text topology intervention`；在 graph encoder、dynamic patch、connector、LLM hidden state 和 final output 五层报告 `Acc@1/MRR`、patch NMI、token count、prediction equality、direction accuracy，并加入 no-RAG、GNN-only、LLM-only、readout-only 和 shuffled graph embedding 对照。检索词：`EDT-Former AMORE equivalent SMILES`、`molecular Graph-LLM SMILES augmentation hidden representation`、`Graph-LLM identity transformation responsibility split`、`fixed atom features topology intervention`。

### 2026-08-29 状态更新说明

- H1：维持“部分支持”。arXiv:2606.09484 是纯序列化 LLM 边界案例：ordinary graph-isomorphism 检测近满分，但同拓扑随机重标记严重失败；这进一步说明 benchmark accuracy 不能直接解释为 Graph-LLM 的结构因果使用，也不能外推到带 GNN encoder 的系统。
- H2：维持“部分支持”并增强直接证据。三种 LLM 在 3 种 serialization 上对 400 个同构重标记对只有 0-39.2% 准确率；但随机图、无节点属性、无 tokenization ablation 的设置仍不足以定位具体捷径来源，也尚未在 GNN-LLM/GTokenLLM 上复现。
- H5：由“待验证”调整为“部分支持”。该论文在同一评测中把 ordinary isomorphism accuracy 与 permutation-invariance 分开，提供了“任务高分与不变量稳定性可脱钩”的正面证据；但没有 fixed-text topology intervention，因此“permutation robustness 与 causal topology use 的可分离性”在 Graph-LLM 上仍待验证。
- H9：维持“待验证”。三种 serialization 的 permutation 结果不同，支持 encoding/order 与结构行为不是单一轴；然而没有 key/irrelevant topology intervention，仍不能完成 encoding robustness 与 topology intervention sensitivity 的联合检验。

2026-08-29 本轮优先级调整：先将 `arXiv:2606.09484` 的 ordinary-isomorphism-versus-relabeling protocol 迁移到 GraphToken/IP-GLLM/HLM-G；对每个模型执行 `10 relabelings × encoding/order variants`，同时记录逐样本 prediction equality、output KL 和 hidden-state distance。随后把 same-text key/irrelevant topology intervention、text shuffle、GNN-only/LLM-only/readout-only 与 held-out topology-family 加入同一批样本；molecular 分支继续复核 EDT-Former/OOD-GraphLLM 的 `3+ valid SMILES`、canonicalization、no-RAG 和 connector 责任。检索词：`Graph-LLM graph isomorphism permutation invariance`、`GTokenLLM prediction equality node relabeling`、`fixed text topology intervention Graph-LLM`、`EDT-Former equivalent SMILES code`。

### 2026-08-29 状态更新说明（GraphTokenizer）

- H1：维持“部分支持”。GraphTokenizer 以 reversible/deterministic discrete interface 取得多域 graph-level 增益，说明结构输入接口本身可以有效；但结构在 Transformer 前已被显式 serialization 和 BPE 编码，不能据此证明 LLM 内部完成了 topology causal computation。
- H2：维持“部分支持”。Feuler/FCPP 通过频率引导和 lexical tie-break 试图消除 traversal ordering ambiguity，是针对 node/edge order shortcut 的正面设计证据；但论文没有逐样本 node relabeling、起点/方向和重复 label 的 empirical equality，因此仍不能把“deterministic”写成已验证的 permutation consistency。
- H3：由“构想”调整为“待验证”。可逆 discrete structural bottleneck、decode fidelity、BPE compression 和多节点结构词表为 H3 提供方法可行性证据；但 continuous features、target addressability、causal intervention 和 responsibility split 尚未解决。
- H5：维持“部分支持”。该论文把 serialization invariance 作为接口属性，与当前研究要求的 final-output causal use 分开；其高分和结构词表可解释性仍不足以跨越从结构输入/可解码到因果使用的证据缺口。

2026-08-29（GraphTokenizer）本轮优先级调整：先用官方代码复核 `Feuler/FCPP` 在重复 labels、起点/方向、edge order、disconnected components 和 `10 relabelings` 下的 serialized/BPE token equality，再把 `decode isomorphism + output KL + prediction equality` 接入 GraphToken/IP-GLLM/HLM-G。之后在相同 Transformer 上加入 fixed-text key/irrelevant topology intervention、text shuffle、GNN-only/readout-only 和 topology-family holdout；若离散 token 接口只改善 standard split 而不能保持等价输出或方向正确干预，则把 H3 降回构想。检索词：`GraphTokenizer Feuler permutation code audit`、`Graph Tokenization BPE isomorphism consistency`、`reversible graph serialization repeated labels`、`Graph-LLM discrete token causal intervention`。

### 2026-08-30 状态更新说明

- H1：维持“部分支持”。Lost in Tokenization 说明即使结构不经过自然语言和 GNN，tokenization 选择也会直接改变 Transformer 的有限深度计算能力；这强化“接口/系统收益不能直接归因于 LLM causal topology use”的边界，但不是 Graph-LLM 实证。
- H2：维持“部分支持”。论文明确区分 permutation-equivariant 的 spectral/random-walk tokenization 与使用 dataset node order 的 adjacency tokenization，给出 order dependence 的接口级证据；仍没有在 GraphTokenizer 或 GTokenLLM 上做逐样本 relabeling equality。
- H3：维持“待验证”并强化设计依据。本文的 local adjacency、global spectral、diffusion random-walk trade-off 和部分 combined-view 收益支持任务条件多视图结构瓶颈值得实验；但没有 addressable discrete token、fixed-text intervention 或 responsibility split，不能把 H3 升为已支持。
- H5：维持“部分支持”。信息充分性、permutation equivariance、有限深度可计算性和最终 causal use 在本文中仍是不同层次；这与 GraphTokenizer 的 reversibility/determinism 和当前研究的 output-level causal test 一致。
- H10：维持“待验证”。论文使用 GraphBench、OGB、ZINC 的 standard split，并未提供 held-out topology-family、generator/transitivity、cross-task 或 Graph-LLM 责任分离证据；多任务覆盖不能替代 topology OOD。

2026-08-30 本轮优先级调整：I1 不再把“加入多视图”单独作为创新主张。先运行 GraphTokenizer 官方 serialization/decode 小规模审计，再在相同 Transformer 和 matched graph pairs 上比较 local edge-covering、global spectral/diffusion、single reversible sequence 与 combined view；所有 view 必须共同通过 `10 relabelings × 4 edge orders × 3 starts/directions`、decode isomorphism、fixed-text key/irrelevant intervention、readout-only/GNN-only 和 held-out topology-family。若 combined 仅提升 standard split 或 token budget，I1 保留为评测协议并降低方法 novelty。检索词：`task-conditioned graph tokenization local global views`、`GraphTokenizer permutation BPE equality`、`graph tokenization finite-depth tradeoff causal intervention`、`Graph-LLM addressable structural token`。

### 2026-08-30 状态更新说明（Colorful Talks with Graphs）

- H1：维持“部分支持”。Colorful Talks 的 ordered 1-WL 和自然颜色词能提高若干 graph-to-prompt 任务，但 WL refinement、全局消息排序和颜色映射均在 LLM 外部完成；任务收益不能直接归因为 LLM 的 causal topology use。
- H2：维持“部分支持”。论文把 permutation-invariant 作为设计目标，却没有逐样本 node relabeling、edge-order、起点/方向或 isomorphism prediction equality；因此不能把形式主张写成已验证的不变量。
- H3：维持“待验证”并强化 shortcut 控制。相同 WL partition 下 natural colors 明显优于 opaque Hue/name tokens，说明离散结构词表的 semantic anchor 必须与结构信息本身分离；这强化 I1 的 lexical-anchor control，但不等于可逆接口证据。
- H5：维持“部分支持”。自然颜色词对最终任务有影响，说明 representation robustness、语义捷径和 topology causal use 是不同轴；论文没有 fixed-text key/irrelevant intervention 或组件责任分离。
- H10：维持“待验证”。BA、ER、Path、长距离图、Cora 等覆盖了图类型和规模压力，但没有 held-out topology-family、generator/transitivity 或 cross-task OOD。

证据账本补充：该论文提供“结构输入”和“结构被使用（任务级间接）”的正面证据，并提供 same-WL partition 的 lexical shortcut control；没有提供“结构反事实”“最终输出 permutation consistency”或“组件责任分离”的直接证据。最重要的新约束是：I1 的 comparison matrix 必须同时包含 ordered-WL natural colors、opaque same-partition tokens、reversible/BPE、Graph-KV mask-only 和 readout-only。

2026-08-30（Colorful Talks with Graphs）本轮优先级调整：先完成 GraphTokenizer serialization/decode audit；随后在 matched token/compute budget 下加入 ordered-WL/color 与 opaque-token 对照，固定节点文本构造 key-edge/irrelevant-edge topology pairs，并联合执行 prediction equality、output KL、direction accuracy、GNN-only/readout-only 和 held-out topology-family。若 natural color 的收益在语义锚点消除或 text shuffle 后消失，I1 只保留为结构评测协议，不主张颜色词方法创新。

### 2026-08-30 状态更新说明（Graph-KV）

- H1：维持“部分支持”。Graph-KV 证明 LLM-side graph-conditioned routing 可以带来任务级收益和上下文/邻居扩展，但其 KV mask、shared PE、retriever 和 post-trained backbone 仍是混合结构，不能把收益归因于 LLM 的独立 causal topology use。
- H2：维持“部分支持”。chunk placement/distractor 的顺序稳健性补充了结构路由的正面证据，但 chunk order 不是 node relabeling 或 graph-isomorphism equality，仍需在 Graph-KV-style mask 上运行逐样本等价审计。
- H5：维持“部分支持”。Graph-KV 的顺序稳健性与 I1 的 permutation/equivalence 轴相关但不等价，进一步说明 robustness、routing 和 causal intervention 必须分别报告。
- H6：维持“待验证”。显式 source→target mask 比 latent soft intervention 更接近结构路由干预，但论文没有 fixed-text key/irrelevant topology pair、方向正确率或模块责任分离。
- H10：维持“待验证”。Arxiv-QA、RAG 和 star-neighbor stress test 是上下文、检索和规模压力测试，不是 held-out topology-family OOD。

2026-08-30（Graph-KV）本轮优先级调整：将 Graph-KV-style mask/shared-PE 作为 mask-only baseline，先完成 GraphTokenizer 官方 serialization/decode audit，再在相同 token/compute budget 下比较 discrete/BPE、continuous projector、raw serialization 与 Graph-KV mask。共同执行 `10 relabelings × 4 edge orders × 3 starts/directions`、decode isomorphism、fixed-text key/irrelevant topology intervention、text shuffle、GNN-only/readout-only 和 held-out topology-family；同时加入 random/irrelevant edge 与 source-edge randomization，避免把 retriever 或位置稳健性误判为结构因果使用。检索词：`Graph-KV graph mask permutation invariance`、`LLM KV cache graph topology intervention`、`Graph-LLM mask-only discrete token comparison`、`fixed-text topology intervention responsibility split`。

### 2026-09-01 状态更新说明（When Graph Tokens Sink）

- H1：维持“部分支持”。该论文直接展示 graph-token activation saliency、attention 和 token-level functional importance 并不一致；这强化“内部显著性或任务增益不能直接证明 LLM 因果使用拓扑”的边界，但没有完成 graph-level causal test。
- H5：维持“部分支持”。sink pruning、swap 和 reposition 是序列/位置干预，不能替代 node relabeling、同构输出 equality 或 fixed-text topology counterfactual；因此进一步支持 robustness、token utility 与 topology causal use 必须分层报告。
- H13：新增并标记“待验证”。sink activation 可能是 `[PAD]`、早期位置、序列稀疏性或 citation-domain pretraining 的产物；需要用 topology-addressable intervention 和 responsibility split 判断它是否能预测真正的结构责任。

2026-09-01 本轮优先级调整：在隔离依赖环境可用前，先把 sink-aware diagnostic 纳入 I1 comparison matrix，报告 sink score、attention share、top-2 sink/non-sink pruning delta、swap/reposition 与 logit-lens vocabulary；随后在相同 matched graph pairs 上联合执行 decode isomorphism、`10 relabelings × 4 edge orders × 3 starts/directions`、fixed-text key/irrelevant topology intervention、text shuffle、GNN-only/LLM-only/readout-only、solver-disabled 和 held-out topology-family。若 sink/attention 只解释 `[PAD]` 或位置现象，或 parser/token utility 不转化为方向正确的 topology intervention，则 I1 继续只作为评测协议。检索词：`graph token mechanistic intervention topology causal responsibility`、`Graph-LLM graph token saliency pruning topology counterfactual`、`fixed text graph token intervention`、`Graph-LLM sink token topology`。
### 2026-09-03 状态更新说明（EstGraph）

- H1：维持“部分支持”。EstGraph 将结构统计输入扩展到 partial-access large graphs，并在若干估计任务上取得有用结果，但 random-walk sampler、统计摘要、classical estimator 与 LLM 责任没有分离，不能把结果升级为 LLM causal topology use。
- H5：维持“部分支持”。节点名随机偏移和不同 walk method 提供了有限 shortcut control，但没有同图 relabeling/output equality；access robustness、scale robustness、permutation consistency 与 topology intervention 仍需分开报告。
- H10：维持“待验证”并增加 partial-access/scale 约束。EstGraph 覆盖 100--100,000-node synthetic 图和最高 2,388,953-node real-world 图，证明大图估计可以成为独立评测轴；但没有 held-out topology-family、generator/transitivity、cross-task 或 responsibility closure。
- H14：维持“部分支持”。四类 EstGraph 任务依赖可汇总的 walk statistics，进一步说明结构收益应按任务与可访问信息条件化；degree/visit-frequency 可能已解释 BA/Grid/PageRank 等部分结果。
- H15：新增并标记“部分支持”。partial-access 与 graph-scale 是独立 OOD 轴，但其有效性必须在 coverage、seed、burn-in、sampling bias 和 degree-only 对照下，和 permutation、fixed-text intervention、topology-family OOD 联合验证。

2026-09-03 本轮优先级调整：I1 保留唯一候选、状态“候选”、评分 8/10；不新增 I2。将 EstGraph 的 MH/SRW、coverage、burn-in、diverse seeds、degree-only/walk-stat-only 和 robust median aggregation 加入 access-aware OOD slice，但不把 random-walk benchmark 单独包装成方法创新。先完成 semantic-sufficiency/structure-essential gate，再运行 GraphTokenizer runtime equality audit；当前 bundled Python 缺少 `torch`、`dgl`、`networkx`、`rdkit`、`transformers`，本轮仅完成官方 release 静态 source audit。下一轮检索与验证：`Graph-LLM partial access topology OOD`、`random walk graph token permutation`、`degree-matched topology counterfactual`、`GraphTokenizer Feuler BPE equality`。

### 2026-09-04 状态更新说明（GenNIE）

- H1：强化为更明确的“任务级结构路径已进入，但因果拓扑利用仍未闭合”。GenNIE 的 Node2Vec、relation-aware GNN、MoE projector 和 topological injection 消融证明结构路径对当前 NIE benchmark 有贡献，但消融同时移除多个模块，不能定位 GNN、projector、LLM 或 readout 的责任。
- H10：维持“待验证”。FB15K 到 MUSIC10K/TMDB5K 的 zero-shot transfer 是 cross-domain transfer，不是 held-out topology-family、generator/transitivity、同任务 graph-scale 或 density OOD。
- H11：强化“责任混合与标签泄漏风险”。label-aware supervised contrastive loss 直接按 ground-truth importance gap 组织 structural embedding；NMI/ARI 主要证明 label-aligned separability，不是未见拓扑属性的解码证据。
- H14：强化“semantic-sufficiency/structure-essential gate 必须前置”。NIE 标签来自 page views、popularity、hotness 和 citations，文本、关系语义、中心性与预训练先验可能共同解释结果；必须在固定文本和 matched budget 下证明结构必要性。
- H15：维持“部分支持”。本文再次表明 cross-domain transfer、partial access、graph scale 与 topology-family OOD 是不同轴；不能用其中一轴替代其余轴。

2026-09-04 本轮优先级调整：I1 继续作为唯一候选，状态“候选”、评分 8/10，不新增 I2。将 GenNIE 的 label-aware contrastive learning、global soft prompt、textual-attribute shortcut 和 topological-injection path confound 纳入 comparison matrix；semantic-sufficiency/structure-essential gate 增加 score-shuffle/random-label、text-shuffle、degree/centrality-only、capacity-matched readout 和 target-label-unseen controls。第一步仍是 CPU matched-task gate，只有 structure-essential 子集显示独立 topology signal 后，才运行 GraphTokenizer 的 relabeling/edge-order/start-direction/decode equality audit。今日论文把 I1 的方法主张进一步收窄为“可证伪的离散接口与责任协议”，不主张新增 MoE、contrastive 或 soft-prompt 方法创新。
