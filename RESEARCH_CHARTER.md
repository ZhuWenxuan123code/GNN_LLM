# GNN-LLM 图结构表征研究纲领

## 核心命题

本研究不再关注如何简单地将图表示输入 LLM，而是研究如何让 LLM 形成可验证、置换一致、因果有效且能够跨拓扑泛化的图结构表征。

## 核心判断标准

只有同时考察以下四个层次，才能讨论模型是否真正获得并利用了图结构：

1. **结构可辨识性（Structural Decodability）**：模型中间表征是否包含可恢复的局部或全局结构信息。
2. **置换一致性（Permutation Consistency）**：节点重编号、边顺序变化或等价序列化后，表示和预测是否保持不变或满足预期等变关系。
3. **因果有效性（Causal Utilization）**：只干预任务相关结构时，模型输出是否发生方向正确的变化；干预无关结构时是否保持稳定。
4. **跨拓扑泛化（Cross-topology Generalization）**：模型能否泛化到训练分布之外的图规模、密度、拓扑族和结构组合。

结构被输入、结构可由 probe 解码、下游指标提高，都不能单独证明结构被模型实际使用。Attention 可视化也不作为充分证据。

## 六条研究证据线

1. 图结构编码与 GNN-LLM 对齐机制。
2. 节点置换不变性与等变性。
3. 图结构表征探针与中间层可解释性。
4. 反事实干预与结构信息的因果利用。
5. 跨图规模、密度和拓扑分布泛化。
6. 排除文本、序列顺序和预训练知识捷径的评测方法。

## 长期研究问题

- RQ1：怎样区分“GNN 已经完成任务、LLM 只负责解码”与真正的语义—结构联合推理？
- RQ2：怎样证明结构信息不只是存在于表示中，而是被最终预测因果性地使用？
- RQ3：怎样设计不依赖节点编号和序列化顺序的 Graph-LLM 接口与训练目标？
- RQ4：怎样构造能够排除文本知识、标签相关性和数据集捷径的反事实评测？
- RQ5：怎样使结构能力泛化到未见图规模、密度、拓扑族和结构组合？
- RQ6：什么样的显式、多尺度、可干预结构瓶颈能够促进 LLM 内部的结构推理？

## 检索边界

纳入直接研究 GNN+LLM、Graph-LLM、graph reasoning 的论文，也纳入能为核心命题提供方法或证据的 Graph Transformer、equivariance、causal representation、mechanistic interpretability、OOD graph generalization、graph benchmark 和 counterfactual evaluation 工作。普通图任务指标论文若没有结构利用证据，只作为对照或负面案例记录。

## 研究产出要求

- 每日初筛候选论文，全文精读 1 篇。
- 中文记录，必要模型名和概念保留英文。
- 所有事实性结论附可访问来源；推断与原文结论明确分开。
- 无法获得全文时只允许做摘要级记录，不得写成全文精读。
- 新 idea 必须说明证据来源、可证伪假设、最小实验和失败条件。
- 每日研究必须继承历史问题并留下下一次可执行的检索问题。

## 稳定性规则

本文件是研究总纲领。每日自动任务只读取，不得自行修改；只有用户明确要求调整研究方向时才更新。
